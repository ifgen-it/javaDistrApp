Что сделать желательно:

- в DAO надо как-то закрывать Connection с БД - при завершении DAO, как в Бине @PreDestroy

- нужно закрывать Registry и скорей всего там закрывать Dao - через Интерфейсный метод возможно,
делать может Сервер удаленно через метод stub

- Сервер по идее справляется в 1м потоке, т.к. не нужно держать соединение, как с TCP
Но при большом колич. клиентов 1го потока будет мало, возможно нужно иметь Пул потоков?

-Пул потоков можно добавить в соединение Сервер-СерверБД, а также пул подключенией в
соединение СерверБД - БД

- многопоточность надо, можно асинхронно принимать пакеты от Клиентов, складывать в очередь, а
в другом потоке разбирать эти пакеты

- Гуй ну Клиенте - Десктоп

- Выделить из Сервера основную бизнес-логику таким образом, чтобы этот новый сервер мог
обслужить легкий Сервер-Десктопных клиентов и легкий Сервер(Сервлет) для веб-клиентов.
